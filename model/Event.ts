export type EventDetailType = {
  location: string;
  date: string;
  time: string;
  activity: string;
  capacity: number;
  price: string;
  eventName: string;
  text: string;
};

export type EventType = {
  id: string;
  title: string;
  description: string;
};
